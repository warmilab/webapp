import React from "react"
import Newsletter from "../../Newsletter"


const NewsletterHome = ({ title, text, url }) => {

  return (
  <section id="newsletter" className="newsletter">
    <Newsletter />
  </section>
)}

export default NewsletterHome
